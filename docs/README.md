# RAG App - Retrieval-Augmented Generation система

Полнофункциональное приложение для индексации и семантического поиска по Markdown документам из Obsidian с интеграцией Yandex GPT для создания RAG (Retrieval-Augmented Generation) агента.

## 🎯 Возможности

### День 15: Индексация документов
- 📚 Рекурсивное сканирование и индексация MD файлов
- 🔪 Умное разбиение текста на чанки с перекрытием
- 🤖 Генерация эмбеддингов через Ollama (модель nomic-embed-text)
- 💾 Хранение векторов в SQLite
- 🔍 Семантический поиск по содержимому

### День 16: RAG агент
- 💬 Интерактивный чат с вечным циклом
- 🔍 **Режим с RAG**: ответы на основе ваших документов
- 🧠 **Режим без RAG**: ответы на основе знаний модели
- ⚖️ **Сравнение режимов**: автоматическое сравнение и анализ
- 📊 Детальная статистика и метрики

## 📋 Требования

1. **Java 23** или выше
2. **Ollama** для генерации эмбеддингов:
   ```bash
   # Установка Ollama (macOS)
   brew install ollama
   
   # Запуск Ollama
   ollama serve
   
   # Загрузка модели для эмбеддингов
   ollama pull nomic-embed-text
   ```

3. **Yandex Cloud API** для GPT:
   - Зарегистрируйтесь на https://cloud.yandex.ru
   - Создайте API ключ
   - Получите Folder ID

## ⚙️ Настройка

### 1. Отредактируйте конфигурационный файл

`app/src/main/resources/application.conf`:

```hocon
ragapp {
    # Путь к папке с Obsidian документами
    obsidian {
        path = "/Users/your-username/Documents/Obsidian Vault"
    }
    
    # Настройки разбиения текста на чанки
    chunking {
        size = 500        # Размер чанка в символах
        overlap = 100     # Перекрытие между чанками
    }
    
    # Настройки Ollama API
    ollama {
        url = "http://localhost:11434"
        model = "nomic-embed-text"
    }
    
    # Настройки хранилища векторов
    storage {
        database-path = "./data/vectors.db"
    }
    
    # Настройки Yandex GPT API
    yandex {
        api-key = "your_yandex_api_key_here"
        folder-id = "your_yandex_folder_id_here"
    }
}
```

**Важно:**
- Укажите правильный путь к вашему хранилищу Obsidian
- Замените `your_yandex_api_key_here` на ваш API ключ
- Замените `your_yandex_folder_id_here` на ваш Folder ID

## 🚀 Запуск

### Способ 1: Через installDist (рекомендуется)

Самый надежный способ для интерактивного приложения:

```bash
# Собрать дистрибутив
./gradlew installDist

# Запустить напрямую
./app/build/install/app/bin/app
```

### Способ 2: Через Gradle

```bash
./gradlew run --console=plain
```

**Важно:** Если приложение не реагирует на ввод, используйте Способ 1.

## 📖 Использование

### Главное меню

После запуска вы увидите меню с командами:

```
╔═══════════════════════════════════════════════════════════════╗
║           RAG App - Retrieval-Augmented Generation          ║
║              День 15-16: Индексация + RAG агент              ║
╚═══════════════════════════════════════════════════════════════╝

═══════════════════════════════════════════════════════════════
📋 Команды:
  1. index    - Индексировать документы
  2. rag      - Чат с использованием RAG
  3. norag    - Чат БЕЗ использования RAG
  4. compare  - Сравнить RAG vs без RAG
  5. stats    - Статистика индекса
  0. exit     - Выход
═══════════════════════════════════════════════════════════════
```

### 1️⃣ Индексация документов

```
Выберите команду: 1
Переиндексировать все документы? (y/n): n
```

Приложение:
- Сканирует вашу папку Obsidian
- Разбивает документы на чанки
- Генерирует эмбеддинги через Ollama
- Сохраняет в SQLite

### 2️⃣ Чат с RAG

```
Выберите команду: 2

💬 Режим чата с RAG (введите 'back' для возврата в меню)

Вопрос: Что такое корутины в Kotlin?
```

Приложение:
1. Генерирует эмбеддинг для вопроса
2. Ищет релевантные фрагменты в ваших документах
3. Формирует контекст из найденных фрагментов
4. Отправляет вопрос + контекст в Yandex GPT
5. Возвращает ответ с источниками

### 3️⃣ Чат без RAG

```
Выберите команду: 3

💬 Режим чата БЕЗ RAG (введите 'back' для возврата в меню)

Вопрос: Что такое корутины в Kotlin?
```

Приложение отправляет вопрос напрямую в Yandex GPT без поиска в базе знаний.

### 4️⃣ Сравнение режимов

```
Выберите команду: 4

🔄 Сравнение режимов RAG vs без RAG

Введите вопрос для сравнения: Как настроить Ktor в моем проекте?
```

Приложение:
- Получает ответ С RAG (с использованием ваших документов)
- Получает ответ БЕЗ RAG (только знания модели)
- Показывает оба ответа side-by-side
- Анализирует и выводит:
  - Где RAG помог (нашел конкретную информацию в ваших документах)
  - Где RAG не помог (нет информации в базе)
  - Разницу во времени ответа
  - Рекомендации по использованию

Пример вывода:

```
╔═══════════════════════════════════════════════════════════════╗
║                     СРАВНЕНИЕ РЕЗУЛЬТАТОВ                      ║
╚═══════════════════════════════════════════════════════════════╝

❓ Вопрос: Как настроить Ktor в моем проекте?

┌─────────────────────────────────────────────────────────────┐
│ 🔍 ОТВЕТ С RAG                                              │
└─────────────────────────────────────────────────────────────┘

На основе ваших заметок, для настройки Ktor добавьте зависимости...

📚 Источники:
  1. kotlin-notes.md (релевантность: 94.56%)
  2. my-project-setup.md (релевантность: 87.23%)

⏱️  Время: 2.3 сек

┌─────────────────────────────────────────────────────────────┐
│ 🧠 ОТВЕТ БЕЗ RAG (только знания модели)                     │
└─────────────────────────────────────────────────────────────┘

Ktor - это фреймворк для создания асинхронных серверов и клиентов...

⏱️  Время: 1.1 сек

╔═══════════════════════════════════════════════════════════════╗
║                          ВЫВОДЫ                                ║
╚═══════════════════════════════════════════════════════════════╝

✅ RAG помог:
  • Ответ основан на конкретных документах из вашей базы
  • Использованы 2 релевантных фрагментов
  • Ответ более специфичен и основан на ваших данных

⏱️  С RAG медленнее на 1.2 сек (из-за поиска в базе)

💡 Рекомендация:
  Используйте RAG для вопросов о ваших документах
```

### 5️⃣ Статистика

```
Выберите команду: 5

📊 Статистика индекса

📈 Данные:
  • Проиндексировано документов: 45
  • Всего чанков: 234
  • Среднее число чанков на документ: 5
```

## 🏗️ Архитектура

```
app/src/main/kotlin/com/arekalov/ragapp/
├── App.kt                         # Интерактивный CLI с вечным циклом
├── config/
│   └── Config.kt                  # Загрузка конфигурации
├── domain/
│   └── Models.kt                  # Модели данных
├── services/
│   ├── DocumentLoader.kt          # Загрузка MD файлов
│   ├── TextChunker.kt             # Разбиение на чанки
│   ├── EmbeddingService.kt        # Ollama эмбеддинги
│   ├── VectorStore.kt             # SQLite хранилище
│   ├── YandexGptService.kt        # Yandex GPT API
│   └── RagAgent.kt                # RAG логика
└── pipeline/
    └── IndexingPipeline.kt        # Оркестрация индексации
```

## 🔬 Технологии

- **Kotlin** - язык программирования
- **Ktor Client** - HTTP клиент для API запросов
- **SQLite** - база данных для векторов
- **Kotlinx Serialization** - JSON сериализация
- **Kotlinx Coroutines** - асинхронность
- **Ollama** + **nomic-embed-text** - генерация эмбеддингов
- **Yandex GPT** - языковая модель для ответов

## 💡 Примеры вопросов

### Хорошо работает С RAG:
- "Какие настройки я использовал для Gradle в моем проекте?"
- "Как я решал проблему с сетью в прошлом месяце?"
- "Что я записал про архитектуру Clean Architecture?"

### Хорошо работает БЕЗ RAG:
- "Что такое Kotlin?"
- "Объясни паттерн Singleton"
- "Как работает сборка мусора в JVM?"

### Интересно сравнить:
- "Как использовать корутины?" (общий вопрос vs ваши заметки)
- "Настройка CI/CD" (если у вас есть заметки по этой теме)

## 🐛 Устранение проблем

### Ошибка: "Ollama API недоступен"

```bash
# Проверьте, что Ollama запущен
ollama serve

# Убедитесь, что модель загружена
ollama list
ollama pull nomic-embed-text
```

### Ошибка: "Yandex GPT API error"

- Проверьте правильность API ключа и Folder ID в конфиге
- Убедитесь, что у вас есть доступ к Yandex Cloud
- Проверьте квоты в Yandex Cloud Console

### Документы не найдены

- Проверьте правильность пути к Obsidian в конфиге
- Убедитесь, что в папке есть `.md` файлы
- Проверьте права доступа к папке

## 📊 Метрики и выводы

### Когда RAG помогает:
✅ Вопросы о специфичной информации из ваших документов  
✅ Поиск конкретных настроек, решений проблем  
✅ Вопросы, требующие цитирования ваших заметок  

### Когда RAG не нужен:
❌ Общие вопросы, не требующие ваших документов  
❌ Вопросы вне контекста ваших заметок  
❌ Теоретические вопросы  

### Производительность:
- Индексация: ~1-2 сек на документ
- Поиск с RAG: ~2-3 сек (включая поиск + GPT)
- Без RAG: ~1-2 сек (только GPT)

## 📝 Лицензия

MIT

## 🙏 Благодарности

- Ollama за локальную генерацию эмбеддингов
- Yandex Cloud за GPT API
- Obsidian за отличную систему заметок
