# RAG App - Система индексации документов

Приложение для индексации и поиска по Markdown документам из Obsidian с использованием локальных эмбеддингов через Ollama.

## Возможности

- 📚 Рекурсивное сканирование и индексация MD файлов
- 🔪 Умное разбиение текста на чанки с перекрытием
- 🤖 Генерация эмбеддингов через Ollama (модель nomic-embed-text)
- 💾 Хранение векторов в SQLite
- 🔍 Семантический поиск по содержимому
- 📊 Статистика индекса

## Требования

1. **Java 23** или выше
2. **Ollama** установлен и запущен:
   ```bash
   # Установка Ollama (macOS)
   brew install ollama
   
   # Запуск Ollama
   ollama serve
   
   # Загрузка модели для эмбеддингов
   ollama pull nomic-embed-text
   ```

## Настройка

Отредактируйте конфигурационный файл `app/src/main/resources/application.conf`:

```hocon
ragapp {
    # Путь к папке с Obsidian документами
    obsidian {
        path = "/path/to/your/obsidian/vault"
    }
    
    # Настройки разбиения текста на чанки
    chunking {
        size = 500        # Размер чанка в символах
        overlap = 100     # Перекрытие между чанками
    }
    
    # Настройки Ollama API
    ollama {
        url = "http://localhost:11434"
        model = "nomic-embed-text"
    }
    
    # Настройки хранилища векторов
    storage {
        database-path = "./data/vectors.db"
    }
}
```

**Важно:** Обязательно укажите правильный путь к вашему хранилищу Obsidian в параметре `obsidian.path`.

## Использование

### 1. Индексация документов

Запустите индексацию всех Markdown файлов:

```bash
./gradlew run --args="index"
```

Для переиндексации всех документов (включая уже проиндексированные):

```bash
./gradlew run --args="index --force"
```

### 2. Поиск по документам

Выполните семантический поиск:

```bash
./gradlew run --args="search kotlin coroutines"
```

Приложение найдёт 5 наиболее релевантных фрагментов и отобразит их с оценкой сходства.

### 3. Статистика индекса

Просмотр статистики проиндексированных документов:

```bash
./gradlew run --args="stats"
```

### 4. Справка

Показать все доступные команды:

```bash
./gradlew run --args="help"
```

## Архитектура

Проект состоит из следующих компонентов:

```
app/src/main/kotlin/com/arekalov/ragapp/
├── App.kt                         # CLI интерфейс
├── config/
│   └── Config.kt                  # Загрузка конфигурации
├── domain/
│   └── Models.kt                  # Модели данных
├── services/
│   ├── DocumentLoader.kt          # Загрузка MD файлов
│   ├── TextChunker.kt             # Разбиение на чанки
│   ├── EmbeddingService.kt        # Генерация эмбеддингов
│   └── VectorStore.kt             # Хранилище векторов (SQLite)
└── pipeline/
    └── IndexingPipeline.kt        # Оркестрация процесса
```

## Технологии

- **Kotlin** - язык программирования
- **Ktor Client** - HTTP клиент для запросов к Ollama
- **SQLite** - база данных для хранения векторов
- **Kotlinx Serialization** - сериализация JSON
- **Ollama** - локальное выполнение моделей
- **nomic-embed-text** - модель для генерации эмбеддингов

## Примеры использования

### Пример 1: Первичная индексация

```bash
# 1. Настройте путь к Obsidian в application.conf
# 2. Убедитесь что Ollama запущен
ollama serve

# 3. Запустите индексацию
./gradlew run --args="index"
```

### Пример 2: Поиск информации

```bash
# Найти информацию о Kotlin
./gradlew run --args="search что такое kotlin"

# Найти информацию о конкретной технологии
./gradlew run --args="search корутины в kotlin"
```

### Пример 3: Переиндексация после изменений

```bash
# После редактирования документов в Obsidian
./gradlew run --args="index --force"
```

## Устранение проблем

### Ollama API недоступен

Если получаете ошибку "Ollama API недоступен":

1. Проверьте, что Ollama запущен: `ollama serve`
2. Убедитесь, что модель загружена: `ollama pull nomic-embed-text`
3. Проверьте URL в конфиге (по умолчанию `http://localhost:11434`)

### Документы не найдены

Если документы не найдены:

1. Проверьте правильность пути в `application.conf`
2. Убедитесь, что в папке есть `.md` файлы
3. Проверьте права доступа к папке

### Ошибки при индексации

Если возникают ошибки при обработке документов:

1. Проверьте кодировку файлов (должна быть UTF-8)
2. Убедитесь, что файлы не повреждены
3. Проверьте логи для деталей

## Расширение функционала

### Изменение размера чанков

В `application.conf` измените параметры:

```hocon
chunking {
    size = 1000      # Увеличить размер чанка
    overlap = 200    # Увеличить перекрытие
}
```

### Использование другой модели

В `application.conf` укажите другую модель Ollama:

```hocon
ollama {
    model = "mxbai-embed-large"  # Другая модель эмбеддингов
}
```

Не забудьте загрузить модель: `ollama pull mxbai-embed-large`

## Лицензия

MIT

